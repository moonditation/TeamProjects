    <?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".list_prom.waiting_promise"
            android:exported="false" />
        <activity
            android:name=".list_prom.complete_promise"
            android:exported="false" />
        <activity
            android:name=".list_prom.active_promise"
            android:exported="false" />
        <activity
            android:name=".friend_list.friend_received"
            android:exported="false" />
        <activity
            android:name=".friend_list.friend_request"
            android:exported="false" />
        <activity
            android:name=".initial_screen.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>